# PLEASE DO NOT COMMIT THIS FILE UNLESS THERE IS A NEW CONFIG TO BE ADDED

# You will need to build like this : cmake -C config.txt

# Path to the GLFW library's main folder (WINDOWS)
set(LOCAL_GLFW_PATH D:/programmes/GLFWSDK)

# Path to the GLM library's folder (WINDOWS)
set(LOCAL_GLM_PATH D:/programmes/GLMSDK/glm)

# Set to true if you are using Visual Studio (WINDOWS)
set(USE_VISUAL_STUDIO true)